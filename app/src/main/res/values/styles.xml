<resources>
  <!-- Base application theme. -->
  <style name="AppTheme" parent="Theme.MaterialComponents">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorSecondary">@color/colorSecondary</item>
    <item name="colorAccent">@color/colorAccent</item>
    <item name="colorOnSurface">@color/colorOnSurface</item>
    <item name="colorOnSecondary">@color/colorOnSecondary</item>
    <item name="android:colorBackground">@color/background</item>
    <item name="windowActionBar">false</item>
    <item name="windowActionModeOverlay">true</item>
    <item name="actionModeStyle">@style/AppTheme.Toolbar</item>
    <item name="windowNoTitle">true</item>
    <item name="colorControlNormal">@color/actionBarIcons</item>
    <item name="actionModeCloseDrawable">@drawable/ic_close_black_24dp</item>
    <item name="android:listDivider">@drawable/divider_line</item>
  </style>

  <style name="BackGroundImageView">
    <item name="android:alpha">1</item>
    <item name="android:scaleType">centerCrop</item>
    <item name="android:src">@drawable/walking_crowd</item>
  </style>

  <style name="MainPageLogo">
    <item name="android:src">@drawable/stalker_logo</item>
  </style>

  <style name="MainPageBackground">
    <item name="android:scaleType">centerCrop</item>
    <item name="android:src">@drawable/background</item>
  </style>

  <style name="AppTheme.Toolbar" parent="Widget.AppCompat.ActionMode">
    <item name="background">@color/colorPrimary</item>
  </style>
</resources>
